Netflix Conductor Server build for PDOK
=======================================

This builds a docker image with just the server component of Conductor.

The included `config.properties` is copied into the image to configure the server.
